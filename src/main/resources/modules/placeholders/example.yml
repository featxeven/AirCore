# AirCore - modules/placeholders
# Docs: https://airdevelopment.gitbook.io/docs/

# --------------------------------------------------
# PLACEHOLDERS
# --------------------------------------------------
placeholders: []
#  # %aircore_key_rank_color%
#  # conditions is a list of strings. Every entry must pass for the block to be
#  # considered satisfied (implicit AND chain across entries). Prefix a single
#  # entry with AND/OR to express compound logic within that one line.
#  #
#  # Entries under "entries" are checked top to bottom - first one whose
#  # conditions all pass wins. An entry with no "conditions" key always
#  # passes - keep it last as the fallback.
#  rank_color:
#    cache: 5                     # Seconds to cache the resolved output; 0 = always evaluate fresh
#    entries:
#      - conditions:
#          - "%luckperms_primary_group_name% == owner"
#        output: "<dark_red>"
#      - conditions:
#          - "%luckperms_primary_group_name% == admin"
#        output: "<dark_purple>"
#      - conditions:
#          - "%luckperms_primary_group_name% == mod"
#        output: "<blue>"
#      - conditions:
#          - "%vault_eco_balance% >= 100000"
#        output: "<gold>"
#      - output: "<gray>"
#
#  # %aircore_key_kit_cooldown_<kit>%   e.g. %aircore_key_kit_cooldown_starter%
#  kit_cooldown:
#    args:
#      - name: kit
#        default: "0"
#    cache: 1
#    entries:
#      - conditions:
#          - "%aircore_kit_cooldown_%kit%_raw% <= 0"
#        output: "<green>Ready"
#      - output: "<yellow>%aircore_kit_cooldown_%kit%%"
#
#  # %aircore_key_afk_time%
#  afk_time:
#    cache: 5
#    entries:
#      - conditions:
#          - "%aircore_afk_active% == false"
#        output: "<gray>-"
#      - output: "<yellow>%aircore_afk_time%"
#
#  # %aircore_key_kd_ratio%
#  # math entries compute a numeric expression instead of a string. The result
#  # is bound to %result%, which transform (or a later reference) can use.
#  # Falls back to a plain "0.00" entry when the player has no deaths yet,
#  # avoiding a divide-by-zero.
#  kd_ratio:
#    cache: 5
#    entries:
#      - conditions:
#          - "%statistic_deaths% == 0"
#        output: "0.00"
#      - math: "%statistic_player_kills% / %statistic_deaths%"
#        transform:
#          - number-format: "0.00"
#
#  # %aircore_key_health_bar%
#  # bar entries render a segmented progress bar from a current/max pair.
#  health_bar:
#    cache: 1
#    entries:
#      - bar:
#          current: "%player_health%"
#          max: "%player_max_health%"
#          segments: 10
#          filled-char: "❤"
#          empty-char: "♡"
#          filled-color: "<red>"
#          empty-color: "<dark_gray>"
#
#  # %aircore_key_xp_bar%
#  # Backed by variables.yml's xp variable, which clamps at max: 1000 and
#  # resets itself to 0 on rank-up - so this bar never needs to hide or
#  # cap itself, it just renders whatever xp currently holds.
#  xp_bar:
#    cache: 2
#    entries:
#      - bar:
#          current: "%aircore_var_xp%"
#          max: "1000"
#          segments: 10
#          filled-char: "■"
#          empty-char: "□"
#          filled-color: "<yellow>"
#          empty-color: "<dark_gray>"
#
#  # %aircore_key_login_greeting%
#  # random entries pick a weighted output. Weight is relative to the total
#  # across the list (here, 1/3 chance each).
#  login_greeting:
#    entries:
#      - random:
#          - weight: 1
#            output: "<green>Welcome back, <white>%player_name%<green>!"
#          - weight: 1
#            output: "<aqua>Good to see you, <white>%player_name%<aqua>."
#          - weight: 1
#            output: "<yellow>The server got better the moment you joined."
#          - weight: 2
#            conditions:
#              - "%luckperms_primary_group_name% == vip"
#            output: "<gold>Welcome back, VIP <white>%player_name%<gold>!"
#
#  # %aircore_key_coins_short%
#  # transform applied to a plain output - truncates a long formatted balance
#  # for tight scoreboard/tab-list space. Shorthand form: truncate: <length>
#  coins_short:
#    cache: 2
#    entries:
#      - output: "%vault_eco_balance_formatted%"
#        transform:
#          - truncate:
#              length: 8
#              suffix: "..."
#
#  # %aircore_key_gamemode_tag%
#  # uppercase forces consistent casing on a value pulled from another plugin,
#  # regardless of how that plugin happens to format it.
#  gamemode_tag:
#    entries:
#      - output: "<gray>%player_gamemode%"
#        transform:
#          - uppercase
#
#  # %aircore_key_world_tag%
#  # replace swaps out raw internal world names for player-facing ones.
#  # Steps run in order, so multiple replace entries can chain.
#  world_tag:
#    entries:
#      - output: "%player_world%"
#        transform:
#          - replace: ["world_nether", "The Nether"]
#          - replace: ["world_the_end", "The End"]
#          - replace: ["world", "Overworld"]
#
#  # %aircore_key_broadcast_preview%
#  # trim removes accidental leading/trailing whitespace from player-submitted
#  # input before truncate hard-caps.
#  broadcast_preview:
#    args:
#      - name: message
#        default: ""
#    entries:
#      - output: "%message%"
#        transform:
#          - trim
#          - truncate: 40